<div class="flex flex-wrap space-y-4 md:flex-nowrap md:space-y-0 md:space-x-10">
  <%= render 'shared/admin_nav' %>
  <div class="flex-grow max-w-3xl mx-auto">
    <h1 class="text-4xl font-bold mb-4"><%= t('add_account') %></h1>
    <%= render 'form' %>
  </div>
</div>
